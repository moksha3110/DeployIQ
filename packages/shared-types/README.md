# shared-types

TypeScript types/DTOs shared between `apps/frontend` and `apps/backend`
(deployment status enums, API request/response shapes) so the two never
drift silently. Scaffolded in Milestone 0.
